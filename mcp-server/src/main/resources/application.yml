server:
  port: 3000

spring:
  application:
    name: hoj-mcp-server

hoj:
  upstream: ${MCP_UPSTREAM:http://hoj-backend:6688}
  api-keys: ${MCP_API_KEYS:}

logging:
  level:
    root: INFO
    top.hcode.hoj.mcp: DEBUG
